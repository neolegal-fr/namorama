version: '3.8'

services:
  api:
    image: git.neolegal.fr/neolegal/namespoter-api:latest
    restart: unless-stopped
    environment:
      DATABASE_URL: mysql://${MARIADB_USER}:${MARIADB_PASSWORD}@192.168.1.95:3306/namespoter
      KEYCLOAK_AUTH_SERVER_URL: ${KEYCLOAK_URL}
      KEYCLOAK_REALM: namespoter
      KEYCLOAK_CLIENT_ID: namespoter-api
      KEYCLOAK_SECRET: ${KEYCLOAK_SECRET}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      FRONTEND_URL: ${FRONTEND_URL}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_ESSENTIAL_PRICE_ID: ${STRIPE_ESSENTIAL_PRICE_ID}
      STRIPE_PACK_PRICE_ID: ${STRIPE_PACK_PRICE_ID}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
    ports:
      - "127.0.0.1:3000:3000"

  web:
    image: git.neolegal.fr/neolegal/namespoter-web:latest
    restart: unless-stopped
    environment:
      API_URL: ${API_URL}
      KEYCLOAK_URL: ${KEYCLOAK_URL}
    ports:
      - "127.0.0.1:8088:80"
    depends_on:
      - api
